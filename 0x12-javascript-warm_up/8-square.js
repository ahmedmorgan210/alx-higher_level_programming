#!/usr/bin/node

const size = Number(process.argv);

if (size) {
  for (let x = 0; x < size; x++) {
    for (let y = 0; y < ; y++) {
      console.log('X');
    }
  }
} else {
  console.log('Missing size');
}
