#!/usr/bin/node

// replace the value 12 with 89
const myObject = {
  type: 'object',
  value: 12
};
console.log(myObject);

// update the needed key (value) of the dictionary
myObject.value = 89;

console.log(myObject);
